name: Pipeline EUDAMED Platform UDI Details Raw

on:
  workflow_dispatch:
    inputs:
      mode:
        description: "Fetch mode"
        required: false
        default: "incremental_current"
        type: choice
        options:
          - incremental
          - resume_incremental
          - incremental_current
          - resume_incremental_current
          - incremental_historical
          - resume_incremental_historical
          - full_current
          - resume_full_current
          - full_historical
          - resume_full_historical
          - full
          - resume_full
      max_rps:
        description: "Max requests per second"
        required: false
        default: "0.8"
      max_records:
        description: "Optional record cap for testing. 0 = no cap"
        required: false
        default: "0"
      log_every_records:
        description: "Progress log cadence in records"
        required: false
        default: "100"
      max_429_before_partial:
        description: "Controlled PARTIAL stop after this many 429 events"
        required: false
        default: "7"
      max_runtime_hours:
        description: "Controlled PARTIAL stop after runtime limit in hours. 0 = disabled"
        required: false
        default: "5.5"
      skip_csv_zip:
        description: "Skip CSV ZIP export"
        required: false
        default: "false"
        type: choice
        options:
          - "false"
          - "true"

permissions:
  contents: write

concurrency:
  group: eudamed-platform-udi-details-raw
  cancel-in-progress: false

env:
  DATA_REPO: Wakzi/EUDAHUB-Intelligence-Data
  OUT_DIR: dist/eudamed_platform_udi_details_raw

jobs:
  fetch-platform-udi-details-raw:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install duckdb pandas requests

      - name: Generate UTC timestamp
        id: timestamp
        run: |
          echo "timestamp=$(date -u +'%Y%m%d_%H%M%S')" >> "$GITHUB_OUTPUT"
          echo "timestamp_human=$(date -u +'%Y-%m-%d %H:%M:%S UTC')" >> "$GITHUB_OUTPUT"

      - name: Prepare inputs from Data repo
        env:
          GH_TOKEN: ${{ secrets.DATA_REPO_TOKEN }}
        run: |
          set +e
          mkdir -p inputs

          gh release download "eudamed-platform-udi-raw-latest" \
            --repo "$DATA_REPO" \
            --pattern "eudamed_platform_udi_raw_latest.duckdb" \
            --dir inputs \
            --clobber || true

          gh release download "eudamed-platform-udi-details-raw-latest" \
            --repo "$DATA_REPO" \
            --pattern "eudamed_platform_udi_details_raw_latest.duckdb" \
            --dir inputs \
            --clobber || true

          gh release download "eudamed-platform-udi-details-raw-latest" \
            --repo "$DATA_REPO" \
            --pattern "eudamed_platform_udi_details_raw_latest.metadata.json" \
            --dir inputs \
            --clobber || true

          ls -lh inputs || true

      - name: Run Platform UDI Details Raw
        run: |
          EXTRA_ARGS=""
          if [ "${{ github.event.inputs.skip_csv_zip || 'false' }}" = "true" ]; then
            EXTRA_ARGS="--skip-csv-zip"
          fi

          python -u pipelines/eudamed/pipeline_eudamed_platform_udi_details_raw.py \
            --mode "${{ github.event.inputs.mode || 'incremental_current' }}" \
            --out-dir "$OUT_DIR" \
            --inputs-dir inputs \
            --release-timestamp "${{ steps.timestamp.outputs.timestamp }}" \
            --max-rps "${{ github.event.inputs.max_rps || '0.8' }}" \
            --max-records "${{ github.event.inputs.max_records || '0' }}" \
            --log-every-records "${{ github.event.inputs.log_every_records || '100' }}" \
            --max-429-before-partial "${{ github.event.inputs.max_429_before_partial || '7' }}" \
            --max-runtime-hours "${{ github.event.inputs.max_runtime_hours || '5.5' }}" \
            $EXTRA_ARGS

      - name: List output
        run: |
          find "$OUT_DIR" -maxdepth 1 -type f -print -exec ls -lh {} \;

      - name: Validate output files
        run: |
          TS="${{ steps.timestamp.outputs.timestamp }}"
          test -f "$OUT_DIR/eudamed_platform_udi_details_raw_latest.duckdb"
          test -f "$OUT_DIR/eudamed_platform_udi_details_raw_latest.metadata.json"
          test -f "$OUT_DIR/RELEASE_NOTES_EUDAMED_PLATFORM_UDI_DETAILS_RAW.md"
          test -f "$OUT_DIR/eudamed_platform_udi_details_raw_${TS}.duckdb"
          test -f "$OUT_DIR/eudamed_platform_udi_details_raw_${TS}.metadata.json"
          test -f "$OUT_DIR/RELEASE_NOTES_EUDAMED_PLATFORM_UDI_DETAILS_RAW_${TS}.md"

      - name: Create dated release in Data repo
        env:
          GH_TOKEN: ${{ secrets.DATA_REPO_TOKEN }}
        run: |
          TS="${{ steps.timestamp.outputs.timestamp }}"
          TAG="eudamed-platform-udi-details-raw-${TS}"
          TITLE="EUDAMED Platform UDI Details Raw - ${{ steps.timestamp.outputs.timestamp_human }}"
          REPO="$DATA_REPO"

          gh release create "$TAG" \
            --repo "$REPO" \
            --title "$TITLE" \
            --notes-file "$OUT_DIR/RELEASE_NOTES_EUDAMED_PLATFORM_UDI_DETAILS_RAW_${TS}.md" \
            --latest=false

          gh release upload "$TAG" \
            "$OUT_DIR/eudamed_platform_udi_details_raw_${TS}.duckdb" \
            "$OUT_DIR/eudamed_platform_udi_details_raw_${TS}.metadata.json" \
            "$OUT_DIR/RELEASE_NOTES_EUDAMED_PLATFORM_UDI_DETAILS_RAW_${TS}.md" \
            --repo "$REPO" \
            --clobber

          if [ -f "$OUT_DIR/eudamed_platform_udi_details_raw_${TS}_csv.zip" ]; then
            gh release upload "$TAG" "$OUT_DIR/eudamed_platform_udi_details_raw_${TS}_csv.zip" --repo "$REPO" --clobber
          fi

      - name: Create latest release in Data repo
        env:
          GH_TOKEN: ${{ secrets.DATA_REPO_TOKEN }}
        run: |
          TAG="eudamed-platform-udi-details-raw-latest"
          TITLE="Latest EUDAMED Platform UDI Details Raw - ${{ steps.timestamp.outputs.timestamp_human }}"
          REPO="$DATA_REPO"

          gh release delete "$TAG" --repo "$REPO" --yes --cleanup-tag || true

          gh release create "$TAG" \
            --repo "$REPO" \
            --title "$TITLE" \
            --notes-file "$OUT_DIR/RELEASE_NOTES_EUDAMED_PLATFORM_UDI_DETAILS_RAW.md" \
            --latest=false

          gh release upload "$TAG" \
            "$OUT_DIR/eudamed_platform_udi_details_raw_latest.duckdb" \
            "$OUT_DIR/eudamed_platform_udi_details_raw_latest.metadata.json" \
            "$OUT_DIR/RELEASE_NOTES_EUDAMED_PLATFORM_UDI_DETAILS_RAW.md" \
            --repo "$REPO" \
            --clobber

          if [ -f "$OUT_DIR/eudamed_platform_udi_details_raw_latest_csv.zip" ]; then
            gh release upload "$TAG" "$OUT_DIR/eudamed_platform_udi_details_raw_latest_csv.zip" --repo "$REPO" --clobber
          fi
